name: benaiah_mobile_app
description: application to allow students to register and view updates from school
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  animated_text_kit: ^4.2.2
  bloc: ^8.1.2
  carousel_slider: ^4.2.1
  cloud_firestore: ^4.9.2
  dartz: ^0.10.1
  device_preview: ^1.1.0
  equatable: ^2.0.5
  firebase_auth: ^4.10.0
  firebase_core: ^2.16.0
  firebase_ui_auth: ^1.10.0
  fl_chart: ^0.64.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  flutter_web_plugins:
    sdk: flutter
  get_it: ^7.6.4
  go_router: ^11.0.0
  http: ^1.1.0
  intl: ^0.18.1

dev_dependencies:
  bloc_test: ^9.1.4
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/icons/
    - assets/images/
